# Replit-specific — never needed in EAS builds
.local/
.cache/
.config/
.canvas/
.upm/
.watchmanconfig
.git/

# Mockup sandbox (255 MB — build tooling only)
artifacts/

# Attached user uploads
attached_assets/

# Replit dev tooling
*.log
/tmp/

# Node modules (EAS installs these fresh)
node_modules/
